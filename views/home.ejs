<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body class="text-white">
    <%- include('./partials/header'); %>
    <div class="w-2/3 mx-auto mt-4 text-center">
        <p class="text-2xl">Welcome back, <%= username %>!</p>
        <% if (lastExpenses.length > 0) { %>
<<<<<<< HEAD
            <table class="w-full text-center border-separate border-spacing-y-2">
                <thead>
                    <tr class="text-sm text-white uppercase tracking-wider">
                        <th class="py-2 bg-surface-a20 rounded-l-lg">Amount</th>
                        <th class="bg-surface-a20">Category</th>
                        <th class="bg-surface-a20">Date</th>
                        <th class="bg-surface-a20 rounded-r-lg">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% lastExpenses.forEach(exp => { %>
                        <tr class="bg-surface-a10 hover:bg-surface-a20 transition">
                            <td class="py-3 rounded-l-lg"><%= exp.amount %> zł</td>
                            <td><%= exp.category %></td>
                            <td><%= exp.date %></td>
                            <td class="py-3 rounded-r-lg">
                                <div class="flex justify-center gap-2">
                                    <form action="/expenses/<%= exp.id %>/change" method="get" class="m-0">
                                        <button type="submit" class="text-primary-a0 hover:underline">Change</button>
                                    </form>
                                    <form action="/expenses/<%= exp.id %>/delete" method="post" onsubmit="return confirm('Delete this record?')" class="m-0">
                                        <button type="submit" class="text-red-400 hover:underline">Delete</button>
                                    </form>
                                </div>
                            </td>                            
                        </tr>
                    <% }) %>
                </tbody>
            </table>
            
            <a href="/expenses" class="text-primary-a0 hover:text-primary-a3    0">View whole history</a>
=======
            <%- include('./partials/table', {expenses: lastExpenses}); %>
            <a href="/expenses/month" class="text-primary-a0 hover:text-primary-a3">View Expenses By Month</a>
            <a href="/expenses" class="text-primary-a0 hover:text-primary-a3 flex justify-center mt-2">View whole history</a>
>>>>>>> dev
        <% } else { %>
            <p>No records</p>
        <% } %>
        

        <form action="/expenses/add" method="post" class="mt-6 w-1/3 mx-auto bg-surface-a10 p-4 rounded-xl">
            <div class="flex flex-row gap-4">
                <input
                    type="number"
                    name="amount"
                    required
                    placeholder="zł"
                    class="w-full px-3 rounded-lg bg-surface-a20 focus:outline-none focus:ring-2 focus:ring-primary-a0">
                <select
                    name="category"
                    required
                    class="w-40 px-3 rounded-lg bg-surface-a20 focus:outline-none focus:ring-2 focus:ring-primary-a0">
                    <option value="">Category</option>
                    <option value="food">Food</option>
                    <option value="clothes">Clothes</option>
                    <option value="transport">Transport</option>
                    <option value="games">Games</option>
                    <option value="restaurants">Restaurants</option>
                    <option value="other">Other</option>
                </select>
        
                <input
                    type="date"
                    name="date"
                    required
                    class="w-36 px-3 py-2 rounded-lg bg-surface-a20 focus:outline-none focus:ring-2 focus:ring-primary-a0">
            </div>
        
            <button type="submit" class="mt-4 py-2 w-full bg-primary-a0 hover:bg-primary-a30 font-semibold rounded-lg transition">Add</button>
        </form>
    </div>

    <script>
        const dateControl = document.querySelector('input[type="date"]');
        dateControl.valueAsDate = new Date();
    </script>
</body>
</html>
